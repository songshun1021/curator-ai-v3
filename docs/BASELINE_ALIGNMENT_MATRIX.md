# Curator AI 基线对齐矩阵（2026-04-09）

## 目标
形成“单一可信基线”：以当前可运行代码为准，文档追平，避免按错文档开发。

## 偏离矩阵（按优先级）

| 优先级 | 偏离主题 | 文档口径（旧） | 当前实现（基线） | 处理决定 |
|---|---|---|---|---|
| P0 | 系统文件路径 | `_*.prompt.md/_*.skill.md` | `/.system/prompt.md` + `/.system/agent.md`（旧路径兼容读取） | 文档统一改为 `/.system/*` |
| P0 | 系统文件可见性 | 文件树可见可编辑 | 文件树默认隐藏，AI配置页高级入口可编辑 | 文档补充“默认隐藏+高级入口” |
| P1 | Chat 上下文注入 | 自由对话固定注入记忆摘要 | 当前为当前文件优先，记忆摘要按场景注入 | 文档改为“按场景注入”并列待增强项 |
| P1 | 复盘上下文 | 强制注入准备包 | 当前复盘链路以原文/JD/简历稳定注入为主 | 文档改为“准备包可选增强” |
| P1 | 导入导出能力 | 完整恢复已完成 | 仍在待完善范围 | 从“已实现”修正为“待实现” |
| P1 | 暗色模式 | 显式入口+持久化已完成 | 仍为部分能力 | 从“已实现”修正为“待实现” |
| P1 | 右键交互 | 菜单式完整交互 | 存在 prompt 临时交互 | 保留现状并列入 UX 升级任务 |
| P2 | 定制简历产物 | JSON + 自动 PDF 成对产出 | JSON 主存储，可视化阅读，PDF按需导出 | 文档修正为“JSON主存储” |
| P2 | 渲染策略 | 打开即源码编辑 | 渲染优先，手动切编辑 | 文档统一为“渲染优先” |
| P2 | AGENTS 规则 | AI内容统一 Markdown | 文书/报告是 MD，结构化资产是 JSON | 增加 JSON 例外条款 |

## 决策冻结

1. `/api/chat` 协议不变。
2. Dexie schema 不变。
3. `ResumeData` 类型结构不变。
4. 本阶段优先修文档与流程，不做技术栈升级。

